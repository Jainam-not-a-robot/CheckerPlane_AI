[project]
name = "checkerplane"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "huggingface-hub[cli]",
    "optimum[onnxruntime]",
    "torch",
    "transformers<4.45.0",
]
