# Models Directory

This directory holds ONNX model exports and tokenizers.
By default, the service runs in **stub mode** with synthetic deterministic inference if model files are not present.

See the root `README.md` under section **"Downloading model weights"** for instructions on exporting real weights via `optimum-cli` and `bench/export_models.py`.
